<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    
    <session-factory>
        <!-- datos de conexion a la base de datos -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost/demo_primefaces_hibernate_mysql</property>
        <property name="connection.username">root</property>
        <property name="connection.password"></property>

        <!-- pool de conexion -->
        <property name="connection.pool_size">1</property>

        <!-- dialecto sql -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <property name="current_session_context_class">thread</property>

        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <!-- log de sentencias ejecutadas -->
        <property name="show_sql">true</property>
        <!-- formatear el log de sentencias ejecutadas -->
        <property name="format_sql">true</property>
        
        <!-- Elimina y vuelve a crear la base de datos -->
        <!--  <property name="hbm2ddl.auto">create</property>  -->

        <mapping resource="data/Persona.hbm.xml" />
        
    </session-factory>

</hibernate-configuration>

